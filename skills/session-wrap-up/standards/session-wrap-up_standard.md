---
date created: 2026-07-15T20:11:16+02:00
date modified: 2026-07-15T20:23:40+02:00
---

## Session Wrap-up Standard

NaN) **Meta-Learning Integrity:** Never overwrite `AGENTS.md` completely. Only append or selectively edit Section 2 (Global Preferences & Bootstrapping) to ensure new standards are linked.

NaN) **Commit Hygiene:** Never run `git push` automatically. Always use `git add .` and `git commit -m "AI Session Wrap-up: ..."`.

NaN) **Single Running Logs:** Project logs must be appended to a single `[Project_Name]_AI_Log.md` file using date-stamped headers. Do not create atomic per-session files.

NaN) **Daily Note Hygiene:** Never use block transclusions (`![[file]]`) in the Daily Note for project logs. Only use standard backlinks (`[[file]]`). Non-project actions should be logged directly in the Daily Note.
